---
const base = import.meta.env.BASE_URL;
---

<nav class="navbar" id="navbar">
  <div class="navbar-inner container">
    <a href={`${base}`} class="navbar-logo">
      <img src={`${base}logo.png`} alt="Attic Pilates" class="logo-img" />
      <div class="logo-text">
        <span class="logo-name">ATTIC</span>
        <span class="logo-sub">PILATES</span>
      </div>
    </a>

    <ul class="navbar-links">
      <li><a href="#clases">Clases</a></li>
      <li><a href="#horarios">Horarios</a></li>
      <li><a href="#precios">Packs</a></li>
      <li><a href="#nosotros">Nosotros</a></li>
      <li><a href="#ubicacion">Ubicación</a></li>
    </ul>

    <div class="navbar-actions">
      <a href="https://www.instagram.com/attic.pilates/" target="_blank" rel="noopener" class="nav-icon" aria-label="Instagram">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <rect x="2" y="2" width="20" height="20" rx="5" ry="5"/>
          <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/>
          <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/>
        </svg>
      </a>
      <a href="#precios" class="btn btn-primary btn-nav">Reservar</a>
    </div>

    <button class="hamburger" id="hamburger" aria-label="Abrir menú">
      <span></span>
      <span></span>
      <span></span>
    </button>
  </div>

  <div class="mobile-menu" id="mobile-menu">
    <ul>
      <li><a href="#clases">Clases</a></li>
      <li><a href="#horarios">Horarios</a></li>
      <li><a href="#precios">Packs</a></li>
      <li><a href="#nosotros">Nosotros</a></li>
      <li><a href="#ubicacion">Ubicación</a></li>
      <li><a href="#precios" class="btn btn-primary" style="margin-top: 1rem; justify-content: center;">Reservar</a></li>
    </ul>
  </div>
</nav>

<style>
  .navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: rgba(242, 239, 230, 0.88);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(45, 41, 38, 0.07);
    transition: all var(--transition);
  }

  .navbar.scrolled {
    background: rgba(242, 239, 230, 0.97);
    box-shadow: 0 1px 12px rgba(45, 41, 38, 0.06);
  }

  .navbar-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.75rem var(--space-md);
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .navbar-logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .logo-img {
    width: 40px;
    height: 40px;
    border-radius: 8px;
    object-fit: cover;
  }

  .logo-text {
    display: flex;
    flex-direction: column;
    line-height: 1.1;
  }

  .logo-name {
    font-family: var(--font-sans);
    font-size: 1.1rem;
    font-weight: 600;
    letter-spacing: 0.22em;
    color: var(--color-dark);
  }

  .logo-sub {
    font-family: var(--font-sans);
    font-size: 0.5rem;
    font-weight: 400;
    letter-spacing: 0.38em;
    color: var(--color-taupe);
    text-transform: uppercase;
  }

  .navbar-links {
    display: flex;
    gap: 2rem;
  }

  .navbar-links a {
    font-size: 0.85rem;
    font-weight: 400;
    letter-spacing: 0.03em;
    color: var(--color-dark-soft);
    position: relative;
  }

  .navbar-links a::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 1px;
    background-color: var(--color-dark);
    transition: width var(--transition);
  }

  .navbar-links a:hover::after {
    width: 100%;
  }

  .navbar-links a:hover {
    color: var(--color-dark);
  }

  .navbar-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .nav-icon {
    color: var(--color-dark-soft);
    transition: color var(--transition);
  }

  .nav-icon:hover {
    color: var(--color-dark);
  }

  .btn-nav {
    padding: 0.6rem 1.5rem;
    font-size: 0.8rem;
  }

  .hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
  }

  .hamburger span {
    display: block;
    width: 24px;
    height: 1.5px;
    background-color: var(--color-dark);
    transition: all var(--transition);
  }

  .hamburger.active span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 4px);
  }

  .hamburger.active span:nth-child(2) {
    opacity: 0;
  }

  .hamburger.active span:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -4px);
  }

  .mobile-menu {
    display: none;
    position: fixed;
    top: 65px;
    left: 0;
    right: 0;
    bottom: 0;
    background: var(--color-bg);
    padding: var(--space-lg);
    z-index: 999;
  }

  .mobile-menu.open {
    display: block;
  }

  .mobile-menu ul {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .mobile-menu a {
    font-family: var(--font-sans);
    font-size: 1.75rem;
    font-weight: 300;
    color: var(--color-dark);
  }

  @media (max-width: 768px) {
    .navbar-links,
    .navbar-actions {
      display: none;
    }

    .hamburger {
      display: flex;
    }
  }
</style>

<script>
  const navbar = document.getElementById('navbar');
  const hamburger = document.getElementById('hamburger');
  const mobileMenu = document.getElementById('mobile-menu');

  window.addEventListener('scroll', () => {
    if (window.scrollY > 50) {
      navbar?.classList.add('scrolled');
    } else {
      navbar?.classList.remove('scrolled');
    }
  });

  hamburger?.addEventListener('click', () => {
    hamburger.classList.toggle('active');
    mobileMenu?.classList.toggle('open');
    document.body.style.overflow = mobileMenu?.classList.contains('open') ? 'hidden' : '';
  });

  document.querySelectorAll('.mobile-menu a').forEach(link => {
    link.addEventListener('click', () => {
      hamburger?.classList.remove('active');
      mobileMenu?.classList.remove('open');
      document.body.style.overflow = '';
    });
  });
</script>
